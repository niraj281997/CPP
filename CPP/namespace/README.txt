

Question 1:
	what is the use of the namespace ?
	namespace basically a packet which may contain variable , function,classes
	
	basically it is use for many porpuse 
	1. To create a packet 
	2. suppose if two namespace using the same variable than it will not gonna
	   crate any problem but all you have to do is use the scope resolution with 
	   the function and variable.

